# AWS Bedrock Configuration (uses AWS SSO profile)
# No API key needed - authentication via AWS SSO
# Run: aws sso login --profile bedrock
AWS_PROFILE=bedrock
AWS_REGION=us-west-2

# Note: Make sure your AWS SSO profile "bedrock" is configured
# and has access to Claude models in AWS Bedrock

# Legacy OpenAI API Key (no longer used, but kept for reference)
# OPENAI_API_KEY=your_openai_api_key_here

# Future: Salesforce API credentials (for Phase 3 - Enterprise Lead Capture)
# SALESFORCE_INSTANCE_URL=https://your-instance.salesforce.com
# SALESFORCE_ACCESS_TOKEN=your_salesforce_token_here
# SALESFORCE_CLIENT_ID=your_client_id
# SALESFORCE_CLIENT_SECRET=your_client_secret

# Future: Sales team email for enterprise lead notifications (Phase 3)
# SALES_EMAIL=sales@yourcompany.com

# Airtable API (if using for pricing data)
# AIRTABLE_API_KEY=your_airtable_api_key
# AIRTABLE_BASE_ID=your_base_id
